import {} from '../action-types';

const initialState = {

};

export default (state = initialState, { type, ...newState }) => {
    switch (type) {
        default:
            return state;
    }
};
